"Loads plugins with vundle
so ~/.config/vim/perso/vundle.vim

"Loads all ”set whatever”
so ~/.config/vim/perso/sets.vim

"Loads abreviations
so ~/.config/vim/perso/ab.vim

"Loads Bépo mapping
"so ~/.config/vim/perso/bepo.vim

"Loads buffers actions
so ~/.config/vim/perso/buffersactions.vim

"Sets beautiful colors
so ~/.config/vim/perso/colors.vim

"Sets useful mapping
so ~/.config/vim/perso/mapping.vim

"Custom plugins config
so ~/.config/vim/perso/pluginsconf.vim

so ~/.config/vim/perso/python-mode.vim

so ~/.config/vim/perso/yaml-mode.vim

"No Backup files
set nobackup
set nowritebackup
set noswapfile

"so ~/.config/vim/perso/xml.vim
